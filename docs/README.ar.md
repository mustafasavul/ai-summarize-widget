```markdown
# AI Summarize Widget ✨

ودجة خفيفة الوزن، صديقة لمحركات البحث وقابلة للتخصيص بدرجة عالية تسمح لزوار موقعك بإرسال مقالاتك إلى مساعدي الذكاء الاصطناعي المفضلين لديهم (ChatGPT، Claude، Gemini، Perplexity) للتلخيص.

مُطوّر بجماليات **Shadcn UI**، واستخراج سياق **JSON-LD (AIO)**، و**Mobile Native Deep Linking**.

## 🚀 الميزات

- 🛡️ **بدون لصق تلقائي:** وظيفة لصق يدوي صارمة تضمن عدم اقتطاع حد عنوان URL.
- 🔗 **دعم AIO (AI SEO):** يستخرج تلقائياً مخطط JSON-LD ووسوم Meta لحقن اسم الموقع والمؤلف وعنوان URL كسياق للذكاء الاصطناعي.
- 🧹 **تنظيف ذكي:** يتجاهل الإعلانات وشريط التنقل والشريط الجانبي والتعليقات. يستخرج فقط نص المقالة النقي.
- 📱 **Mobile Deep Linking:** يتجاوز حاصرات النوافذ المنبثقة على الجوال ويفتح تطبيقات الذكاء الاصطناعي الأصلية (iOS/Android) بسلاسة.
- 🌍 **متعدد اللغات وRTL:** يكتشف تلقائياً `en`، `tr`، `es`، `zh`، `de`، `fr`، `it`، `pt`، `ru`، `nl`. يدعم بالكامل تخطيطات RTL (`ar`، `fa`، `he`، `ur`).
- 🎨 **بدون تسرب CSS:** أنماط مغلفة بالكامل لن تتعارض أبداً مع CSS موقعك.

---

## 📦 التثبيت

### الطريقة 1: عبر CDN (الأسهل لـ HTML/JS العادي)
أضف هذا السكربت إلى HTML قبل وسم الإغلاق `</body>`:

```html
<script src="[https://unpkg.com/ai-summarize-widget/dist/ai-summarize-widget.min.js](https://unpkg.com/ai-summarize-widget/dist/ai-summarize-widget.min.js)"></script>

```

### الطريقة 2: عبر NPM (لـ React، Vue، Next.js)

```bash
npm install ai-summarize-widget

```

```javascript
import AISummarizeWidget from 'ai-summarize-widget';

```

---

## 💻 الاستخدام وأنواع التنفيذ

يمكنك دمج هذه الودجة في موقعك بطريقتين مختلفتين حسب احتياجاتك من واجهة المستخدم.

### النوع 1: زر عائم ثابت ونافذة منبثقة (افتراضي)

يضع زر إجراء عائم (FAB) في زاوية الشاشة. عند النقر، يفتح نافذة منبثقة أنيقة في المنتصف. مثالي للمدونات ومقالات الأخبار الطويلة.

```html
<script>
  new AISummarizeWidget({
    type: 'fixed',
    position: 'bottom-right', // 'bottom-left', 'top-right', 'top-left'
    buttonColor: '#4f46e5',   // لون علامتك التجارية
    lang: 'ar'                // فرض العربية (اختياري)
  });
</script>

```

### النوع 2: زر مضمن ونافذة منبثقة

يحقن الزر مباشرة في جزء محدد من DOM (مثل بجانب أزرار المشاركة الاجتماعية أو رأس المقالة). عند النقر، يفتح نافذة منبثقة سياقية ملحقة بالزر.

```html
<div class="my-share-buttons">
    <button>مشاركة على X</button>
    </div>

<script>
  new AISummarizeWidget({
    type: 'inline',
    target: '.my-share-buttons', // محدد CSS حيث يجب أن يظهر الزر
    buttonColor: '#10b981'
  });
</script>

```

---

## ⚙️ خيارات التكوين

يمكنك تخصيص الودجة بسهولة بتمرير كائن خيارات أثناء التهيئة.

| الخيار | النوع | الافتراضي | الوصف |
| --- | --- | --- | --- |
| **`type`** | `String` | `'fixed'` | وضع العرض. استخدم `'fixed'` لزر/نافذة عائمة، أو `'inline'` لحقنها في حاوية محددة مع نافذة منبثقة. |
| **`target`** | `String` | `null` | **مطلوب إذا كان `type` هو `'inline'`.** محدد CSS لعنصر DOM حيث سيُضاف الزر (مثل `'.article-actions'`). |
| **`position`** | `String` | `'bottom-right'` | الموضع للنوع `'fixed'`. الخيارات: `'bottom-right'`، `'bottom-left'`، `'top-right'`، `'top-left'`. |
| **`buttonColor`** | `String` | `'#4f46e5'` | رمز اللون HEX أو RGB لأزرار الودجة وأشرطة التقدم لتطابق هوية علامتك التجارية. |
| **`lang`** | `String` | *يُكتشف تلقائياً* | يفرض لغة محددة. إذا حُذف، يقرأ تلقائياً سمة `<html lang="x">` أو لغة متصفح المستخدم. |
| **`redirectDelay`** | `Number` | `1200` | التأخير (بالميلي ثانية) قبل إعادة توجيه المستخدم إلى مساعد الذكاء الاصطناعي، مما يسمح له برؤية رسوم "تم النسخ" التوست. |

---

## 📄 الترخيص

رخصة MIT © 2026
```
